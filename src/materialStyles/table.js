import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    tableContainer: {
    },
    button: {
        minWidth: "80px",
        height: "25px",
        fontSize: "10px",
        marginTop: "2px"
    },
    tableRow: {
    },
    tableCell: {
    }
})
